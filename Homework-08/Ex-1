# Завдання 1
# Зробіть рефакторинг наступної програми (тобто використайте list comprehension і елементи функціонального програмування (map, filter))

import random

list_with_numbers = list(range(10))
new_list = []
for i in list_with_numbers:
    if not i % 2:
        new_list.append(i + random.random())
print(new_list)

# Класичний 'list comprehension'
print([(i + random.random()) for i in list_with_numbers if not i % 2])


def foo():
    lis = [i for i in list_with_numbers if not i % 2]
    return lis


# 'list comprehension' за допомогою функції 'map'
print(list(map(lambda x: x + random.random(), foo())))

# 'list comprehension' за допомогою функції 'filter'
print(list(filter(None, new_list)))
